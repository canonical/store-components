import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import LoadingCard from "./LoadingCard";

<Meta title="LoadingCard" component={LoadingCard} />

export const Template = (args) => <LoadingCard {...args} />;

# LoadingCard

This is a [React](https://reactjs.org/) component for a default package card.

## Props

<ArgsTable of={LoadingCard} />

## Default

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

## Custom height

<Canvas>
  <Story name="Custom height" args={{ height: 120 }}>
    {Template.bind({})}
  </Story>
</Canvas>
