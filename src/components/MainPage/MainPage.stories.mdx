import { Canvas, Meta, Story } from "@storybook/addon-docs";

import MainPage from "./MainPage";

<Meta title="MainPage" component={MainPage} />

export const Template = (args) => <MainPage {...args} />;

### MainPage

This is an example of a cards list page.

### Props

### Default

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>
